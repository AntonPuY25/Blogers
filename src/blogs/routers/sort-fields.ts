export enum SortFields {
  CreatedAt = "createdAt",
  Title = "title",
  BlogName = "blogName",
  Name = "name",
}

export enum SortFieldsForUsers {
  CreatedAt = "createdAt",
  Email = "email",
  Login = "login",
}

export enum SortFieldsForComments {
  Content = "content",
  CreatedAt = "createdAt",
}
